Steps:

  1. Create a file called `program.pyx`
  2. Create a file called `setup.py`
  3. Create a testing file called `testing.py`
  
  Copy my `program.pyx` file contents into yours.
  Copy my `setup.py` file contents into yours.
  
  now run
  
  `python setup.py build_ext --inplace`
  
  Finally copy the contents of `testing.py` into your file.
  
  run `python testing.py`
